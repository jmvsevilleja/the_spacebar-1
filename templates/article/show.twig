{% extends 'base.twig' %}

{% block title %}
Read "{{ title }}"
{% endblock %}


{% block body %}

{{ dump() }}

    <h1>{{ title }}</h1>

    <h3>comments <em>({{ comments|length }})</em>:</h3>

    <ul>
    {% for comment in comments %}
        <li>{{ comment }}</li>
    {% endfor %}
    </ul>

{% endblock %}